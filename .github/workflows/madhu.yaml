- name: Setup SSH
  run: |
    mkdir -p ~/.ssh
    echo "${{ secrets.VM_SSH_KEY }}" > ~/.ssh/id_ed25519
    chmod 600 ~/.ssh/id_ed25519

- name: Test SSH
  run: |
    ssh -i ~/.ssh/id_ed25519 ubuntu@${{ secrets.VM_HOST }} "echo CONNECTED"
